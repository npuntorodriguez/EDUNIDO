services:
  - type: web
    name: edunido
    env: python
    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
    startCommand: gunicorn PortfolioProject.wsgi:application
    releaseCommand: python manage.py migrate